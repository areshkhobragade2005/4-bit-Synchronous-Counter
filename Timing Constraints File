# Clock definition (100 MHz)
create_clock -period 10.000 -name clk [get_ports clk]

# Input delay
set_input_delay 2.0 -clock clk [get_ports rst]

# Output delay
set_output_delay 2.0 -clock clk [get_ports q[*]]

# Clock uncertainty
set_clock_uncertainty 0.2 [get_clocks clk]
